# Supabase Configuration
# Update these with your Supabase project details

# Public URL (for Auth and API calls)
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co

# Anon Key (Safe for client-side)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Service Role Key (Server-side ONLY - used for tenant:build seeds if needed, or admin tasks)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Optional: Project Ref if needed for scripts
SUPABASE_PROJECT_REF=your-project-ref

# App Config
APP_URL=http://localhost:3000
